[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "minigym"
version = "0.1.0"
description = "Minimalist safety gym clone"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    # TODO
]

[tool.setuptools.packages.find]
where = ["src"]
